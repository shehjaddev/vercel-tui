version: 2

project_name: vtui

builds:
  - id: vtui
    main: ./cmd/vtui
    binary: vtui
    env: [CGO_ENABLED=0]
    goos: [linux, darwin]
    goarch: [amd64, arm64]

archives:
  - formats: [tar.gz]

checksum:
  name_template: checksums.txt

changelog:
  disable: true
